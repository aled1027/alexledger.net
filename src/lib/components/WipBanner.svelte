<section class="wip-banner" role="status" aria-live="polite">
	⚠️ Portfolio is a work in progress.
</section>

<style>
	.wip-banner {
		position: fixed;
		top: 0.5rem;
		left: 50%;
		transform: translateX(-50%);
		z-index: 20;
		width: fit-content;
		padding: 0.5rem 0.75rem;
		border-radius: 999px;
		border: 1px solid color-mix(in srgb, var(--color-color-0) 55%, transparent);
		background: color-mix(in srgb, var(--color-primary-900) 85%, black);
		color: white;
		font-size: var(--size-step--2);
		font-weight: 600;
		backdrop-filter: blur(6px);
	}
</style>
